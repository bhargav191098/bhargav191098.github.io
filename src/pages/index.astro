---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog")).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-xl py-1">Hey there 👋</div>
    <div class="text-5xl font-bold">I'm Bhargav Krishnamurthy</div>
    <!--<div class="text-3xl py-3 font-bold">AI Software Engineer at Nvidia</div>-->
    <div class="py-2">
      <text class="text-lg">
        <br>Passionate about the realms of <b> Deep Learning, Machine Learning, and Artificial Intelligence Systems</b>, I find immense joy in the art of crafting scalable software systems.
<br><br>
I had the privilege of spending two years at Samsung Research, Bangalore, contributing to the SmartThings Application on WearOS Galaxy watches. I've been fortunate to play a part in innovations that have led to the recent commercialization of several patents and novel features.
<br><br>
I look at Deep learning as a craft of sorts. It is a multifaceted problem statement. To become a good craftsman at one of its facets, is one of my long term dreams. Gonna fail a lot, learn a lot. Nvidia is a great place to do this I believe.
</ul>
    </div>
    <div class="mt-8">
      <a class="btn" href="https://www.linkedin.com/in/bhargav-krishnamurthy/" target="_blank"> Let's connect!</a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-5 mt-10">Latest from blog</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.data.title}
          img={post.data.heroImage}
          desc={post.data.description}
          url={"/blog/" + post.slug}
          target="_self"
          badge={post.data.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  }
</BaseLayout>
